#include <stdio.h>
#include <string.h>

int main() {
    char exp[20];
    printf("Enter expression: ");
    scanf("%s", exp);

    printf("Operator precedence parsing not fully implemented,\n");
    printf("but expression received: %s\n", exp);

    return 0;
}